export type State = {
    field: string
};

const initial: State = {
    field: 'add possible ui states in this file'
};

export type Other = {
    some: string
};

const other: Other = {
    some: 'otherfield'
};

export default {
    initial,
    other,
};
